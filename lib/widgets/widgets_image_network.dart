import 'package:flutter/material.dart';

class WidgetImageNetworks extends StatelessWidget {
  const WidgetImageNetworks({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
